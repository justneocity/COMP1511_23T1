#include <stdio.h>

int main(void) {

    // 4 KEY Parts of a loop

    // 1. INITIALISATION
    int i = 0;

    // 2. CONDITION
    while (i < 10) {

    // 3. BODY 
        printf("%d\n", i);    

    // 4. INCREMENTATION
        // i = i + 1;
        i++;
    }

    return 0;
}
